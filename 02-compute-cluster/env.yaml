$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: sft-finetune-cuda126
version: cli-test-1
description: CLI smoke test of BuildContext upload + ACR build (custom ACPT fine-tune env).
build:
  path: ./container
  dockerfile_path: Dockerfile
tags:
  app: finetuning
  domain: ml
  base: acpt-cu126-py314-torch212
  origin: az-cli-test
